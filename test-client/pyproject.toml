[project]
name = "oauth2-test-client"
version = "0.1.0"
description = "Test client for OAuth2.0 SSO"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.30.0",
    "httpx>=0.27.0",
    "jinja2>=3.1.4",
    "python-multipart>=0.0.9",
]

[project.scripts]
start-client = "main:start"
